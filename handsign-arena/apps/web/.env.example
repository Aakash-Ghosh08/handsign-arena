# URL of the realtime WebSocket server (apps/server), reachable from the browser.
# Local dev default (if unset) is ws://<current hostname>:8080
NEXT_PUBLIC_REALTIME_URL=wss://your-realtime-server.example.com
